# defaults file for address-monitor

# Interfaces to monitor for address changes.
# If not set, all interfaces will be monitored.
# If uncommented, it must contain a space-separated list of interface
# names to monitor,
#INTERFACES="enp4s0 enp9s0"

# Extra options to pass to the daemon.
EXTRA_OPTIONS=""

# External command to execute when an address change has been discovered.
# This must be set, otherwise the daemon won't start.
COMMAND="/usr/local/sbin/address-monitor-helper"
